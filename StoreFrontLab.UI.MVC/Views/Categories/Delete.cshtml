@model StoreFrontLab.DATA.EF.Category

@{
    ViewBag.Title = "Deleting: "+Model.CategoryName;
}
<div class="offset-lg-2 col-lg-8 order-md-1 order-1">
    <h2>@ViewBag.Title</h2>
    <br />
    <h3>Are you sure you want to delete the @Model.CategoryName category?</h3>
    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.CategoryName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.CategoryName)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete @Model.CategoryName" class="btn  btn-danger pull-left" />
                @Html.ActionLink("Cancel", "Index", null, new { @class = "btn btn-outline-success pull-right" })
            </div>
        }
    </div>
</div>